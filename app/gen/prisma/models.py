# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template models.py.jinja --
import os
import logging
import inspect
import warnings
from collections import OrderedDict

from pydantic import BaseModel, Field

from . import types, enums, errors, fields, bases
from ._types import FuncType
from ._compat import model_rebuild, field_validator
from ._builder import serialize_base64
from .generator import partial_models_ctx, PartialModelField


log: logging.Logger = logging.getLogger(__name__)
_created_partial_types: Set[str] = set()

class Industry(bases.BaseIndustry):
    """Represents a Industry record"""

    industry_id: _int
    name: _str
    description: Optional[_str] = None
    jobs: Optional[List['models.Job']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.IndustryKeys']] = None,
        exclude: Optional[Iterable['types.IndustryKeys']] = None,
        required: Optional[Iterable['types.IndustryKeys']] = None,
        optional: Optional[Iterable['types.IndustryKeys']] = None,
        relations: Optional[Mapping['types.IndustryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.IndustryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Industry_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Industry_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Industry_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Industry_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Industry_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Industry_relational_fields:
                        raise errors.UnknownRelationalFieldError('Industry', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Industry / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Industry',
            }
        )
        _created_partial_types.add(name)


class JobCategory(bases.BaseJobCategory):
    """Represents a JobCategory record"""

    category_id: _int
    name: _str
    description: Optional[_str] = None
    jobs: Optional[List['models.Job']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.JobCategoryKeys']] = None,
        exclude: Optional[Iterable['types.JobCategoryKeys']] = None,
        required: Optional[Iterable['types.JobCategoryKeys']] = None,
        optional: Optional[Iterable['types.JobCategoryKeys']] = None,
        relations: Optional[Mapping['types.JobCategoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.JobCategoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _JobCategory_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _JobCategory_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _JobCategory_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _JobCategory_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _JobCategory_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _JobCategory_relational_fields:
                        raise errors.UnknownRelationalFieldError('JobCategory', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid JobCategory / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'JobCategory',
            }
        )
        _created_partial_types.add(name)


class Skill(bases.BaseSkill):
    """Represents a Skill record"""

    skill_id: _int
    name: _str
    feedback_skills: Optional[List['models.FeedbackSkill']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.SkillKeys']] = None,
        exclude: Optional[Iterable['types.SkillKeys']] = None,
        required: Optional[Iterable['types.SkillKeys']] = None,
        optional: Optional[Iterable['types.SkillKeys']] = None,
        relations: Optional[Mapping['types.SkillRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.SkillKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Skill_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Skill_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Skill_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Skill_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Skill_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Skill_relational_fields:
                        raise errors.UnknownRelationalFieldError('Skill', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Skill / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Skill',
            }
        )
        _created_partial_types.add(name)


class Certification(bases.BaseCertification):
    """Represents a Certification record"""

    certification_id: _int
    name: _str
    feedback_certifications: Optional[List['models.FeedbackCertification']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CertificationKeys']] = None,
        exclude: Optional[Iterable['types.CertificationKeys']] = None,
        required: Optional[Iterable['types.CertificationKeys']] = None,
        optional: Optional[Iterable['types.CertificationKeys']] = None,
        relations: Optional[Mapping['types.CertificationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CertificationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Certification_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Certification_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Certification_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Certification_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Certification_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Certification_relational_fields:
                        raise errors.UnknownRelationalFieldError('Certification', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Certification / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Certification',
            }
        )
        _created_partial_types.add(name)


class Company(bases.BaseCompany):
    """Represents a Company record"""

    company_id: _int
    name: _str
    feedback_companies: Optional[List['models.FeedbackCompany']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.CompanyKeys']] = None,
        exclude: Optional[Iterable['types.CompanyKeys']] = None,
        required: Optional[Iterable['types.CompanyKeys']] = None,
        optional: Optional[Iterable['types.CompanyKeys']] = None,
        relations: Optional[Mapping['types.CompanyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.CompanyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Company_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Company_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Company_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Company_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Company_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Company_relational_fields:
                        raise errors.UnknownRelationalFieldError('Company', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Company / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Company',
            }
        )
        _created_partial_types.add(name)


class Talent(bases.BaseTalent):
    """Represents a Talent record"""

    talent_id: _int
    name: _str
    feedback_talents: Optional[List['models.FeedbackTalent']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.TalentKeys']] = None,
        exclude: Optional[Iterable['types.TalentKeys']] = None,
        required: Optional[Iterable['types.TalentKeys']] = None,
        optional: Optional[Iterable['types.TalentKeys']] = None,
        relations: Optional[Mapping['types.TalentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.TalentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Talent_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Talent_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Talent_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Talent_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Talent_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Talent_relational_fields:
                        raise errors.UnknownRelationalFieldError('Talent', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Talent / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Talent',
            }
        )
        _created_partial_types.add(name)


class Interest(bases.BaseInterest):
    """Represents a Interest record"""

    interest_id: _int
    name: _str
    feedback_interests: Optional[List['models.FeedbackInterest']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.InterestKeys']] = None,
        exclude: Optional[Iterable['types.InterestKeys']] = None,
        required: Optional[Iterable['types.InterestKeys']] = None,
        optional: Optional[Iterable['types.InterestKeys']] = None,
        relations: Optional[Mapping['types.InterestRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.InterestKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Interest_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Interest_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Interest_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Interest_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Interest_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Interest_relational_fields:
                        raise errors.UnknownRelationalFieldError('Interest', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Interest / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Interest',
            }
        )
        _created_partial_types.add(name)


class Job(bases.BaseJob):
    """Represents a Job record"""

    job_id: _int
    industry_id: _int
    category_id: Optional[_int] = None
    name: _str
    description: Optional[_str] = None
    updated_at: datetime.datetime
    created_at: datetime.datetime
    industry: Optional['models.Industry'] = None
    category: Optional['models.JobCategory'] = None
    job_images: Optional[List['models.JobImage']] = None
    job_feedbacks: Optional[List['models.JobFeedback']] = None
    histories: Optional[List['models.History']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.JobKeys']] = None,
        exclude: Optional[Iterable['types.JobKeys']] = None,
        required: Optional[Iterable['types.JobKeys']] = None,
        optional: Optional[Iterable['types.JobKeys']] = None,
        relations: Optional[Mapping['types.JobRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.JobKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Job_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Job_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Job_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Job_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Job_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Job_relational_fields:
                        raise errors.UnknownRelationalFieldError('Job', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Job / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Job',
            }
        )
        _created_partial_types.add(name)


class JobImage(bases.BaseJobImage):
    """Represents a JobImage record"""

    img_id: _int
    job_id: _int
    img_url: _str
    alt: _str
    created_at: datetime.datetime
    job: Optional['models.Job'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.JobImageKeys']] = None,
        exclude: Optional[Iterable['types.JobImageKeys']] = None,
        required: Optional[Iterable['types.JobImageKeys']] = None,
        optional: Optional[Iterable['types.JobImageKeys']] = None,
        relations: Optional[Mapping['types.JobImageRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.JobImageKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _JobImage_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _JobImage_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _JobImage_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _JobImage_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _JobImage_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _JobImage_relational_fields:
                        raise errors.UnknownRelationalFieldError('JobImage', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid JobImage / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'JobImage',
            }
        )
        _created_partial_types.add(name)


class JobFeedback(bases.BaseJobFeedback):
    """Represents a JobFeedback record"""

    feedback_id: _str
    job_id: _int
    salary: Optional[_int] = None
    level: Optional[_int] = None
    end_time: Optional[datetime.datetime] = None
    holiday: Optional[_float] = None
    overtime_hours: Optional[_int] = None
    age: Optional[_int] = None
    tenure_years: Optional[_float] = None
    marriage_age: Optional[_int] = None
    gender_ratio: Optional[_float] = None
    romance_rate: Optional[_float] = None
    social_signification: Optional[_str] = None
    personality_traits: Optional[_str] = None
    growth_opportunities: Optional[_str] = None
    wrong_image: Optional[_str] = None
    uniform: Optional[_bool] = None
    work_life_balance: Optional[_float] = None
    future_outlook: Optional[_str] = None
    rarity: Optional[_float] = None
    scandal_history: Optional[_str] = None
    focus_on_education: Optional[_bool] = None
    focus_on_achievements: Optional[_bool] = None
    appeal_points: Optional[_str] = None
    daily_routine: Optional[_str] = None
    comments: Optional[_str] = None
    updated_at: datetime.datetime
    created_at: datetime.datetime
    job: Optional['models.Job'] = None
    feedback_skill: Optional[List['models.FeedbackSkill']] = None
    feedback_certification: Optional[List['models.FeedbackCertification']] = None
    feedback_company: Optional[List['models.FeedbackCompany']] = None
    feedback_talent: Optional[List['models.FeedbackTalent']] = None
    feedback_interest: Optional[List['models.FeedbackInterest']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.JobFeedbackKeys']] = None,
        exclude: Optional[Iterable['types.JobFeedbackKeys']] = None,
        required: Optional[Iterable['types.JobFeedbackKeys']] = None,
        optional: Optional[Iterable['types.JobFeedbackKeys']] = None,
        relations: Optional[Mapping['types.JobFeedbackRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.JobFeedbackKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _JobFeedback_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _JobFeedback_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _JobFeedback_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _JobFeedback_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _JobFeedback_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _JobFeedback_relational_fields:
                        raise errors.UnknownRelationalFieldError('JobFeedback', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid JobFeedback / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'JobFeedback',
            }
        )
        _created_partial_types.add(name)


class FeedbackSkill(bases.BaseFeedbackSkill):
    """Represents a FeedbackSkill record"""

    feedback_id: _str
    skill_id: _int
    is_required: _bool
    created_at: datetime.datetime
    feedback: Optional['models.JobFeedback'] = None
    skill: Optional['models.Skill'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FeedbackSkillKeys']] = None,
        exclude: Optional[Iterable['types.FeedbackSkillKeys']] = None,
        required: Optional[Iterable['types.FeedbackSkillKeys']] = None,
        optional: Optional[Iterable['types.FeedbackSkillKeys']] = None,
        relations: Optional[Mapping['types.FeedbackSkillRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FeedbackSkillKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FeedbackSkill_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FeedbackSkill_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FeedbackSkill_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FeedbackSkill_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FeedbackSkill_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FeedbackSkill_relational_fields:
                        raise errors.UnknownRelationalFieldError('FeedbackSkill', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FeedbackSkill / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FeedbackSkill',
            }
        )
        _created_partial_types.add(name)


class FeedbackCertification(bases.BaseFeedbackCertification):
    """Represents a FeedbackCertification record"""

    feedback_id: _str
    certification_id: _int
    is_required: _bool
    created_at: datetime.datetime
    feedback: Optional['models.JobFeedback'] = None
    certification: Optional['models.Certification'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FeedbackCertificationKeys']] = None,
        exclude: Optional[Iterable['types.FeedbackCertificationKeys']] = None,
        required: Optional[Iterable['types.FeedbackCertificationKeys']] = None,
        optional: Optional[Iterable['types.FeedbackCertificationKeys']] = None,
        relations: Optional[Mapping['types.FeedbackCertificationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FeedbackCertificationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FeedbackCertification_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FeedbackCertification_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FeedbackCertification_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FeedbackCertification_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FeedbackCertification_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FeedbackCertification_relational_fields:
                        raise errors.UnknownRelationalFieldError('FeedbackCertification', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FeedbackCertification / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FeedbackCertification',
            }
        )
        _created_partial_types.add(name)


class FeedbackCompany(bases.BaseFeedbackCompany):
    """Represents a FeedbackCompany record"""

    feedback_id: _str
    company_id: _int
    created_at: datetime.datetime
    feedback: Optional['models.JobFeedback'] = None
    company: Optional['models.Company'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FeedbackCompanyKeys']] = None,
        exclude: Optional[Iterable['types.FeedbackCompanyKeys']] = None,
        required: Optional[Iterable['types.FeedbackCompanyKeys']] = None,
        optional: Optional[Iterable['types.FeedbackCompanyKeys']] = None,
        relations: Optional[Mapping['types.FeedbackCompanyRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FeedbackCompanyKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FeedbackCompany_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FeedbackCompany_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FeedbackCompany_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FeedbackCompany_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FeedbackCompany_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FeedbackCompany_relational_fields:
                        raise errors.UnknownRelationalFieldError('FeedbackCompany', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FeedbackCompany / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FeedbackCompany',
            }
        )
        _created_partial_types.add(name)


class FeedbackTalent(bases.BaseFeedbackTalent):
    """Represents a FeedbackTalent record"""

    feedback_id: _str
    talent_id: _int
    is_required: _bool
    created_at: datetime.datetime
    feedback: Optional['models.JobFeedback'] = None
    talent: Optional['models.Talent'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FeedbackTalentKeys']] = None,
        exclude: Optional[Iterable['types.FeedbackTalentKeys']] = None,
        required: Optional[Iterable['types.FeedbackTalentKeys']] = None,
        optional: Optional[Iterable['types.FeedbackTalentKeys']] = None,
        relations: Optional[Mapping['types.FeedbackTalentRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FeedbackTalentKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FeedbackTalent_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FeedbackTalent_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FeedbackTalent_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FeedbackTalent_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FeedbackTalent_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FeedbackTalent_relational_fields:
                        raise errors.UnknownRelationalFieldError('FeedbackTalent', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FeedbackTalent / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FeedbackTalent',
            }
        )
        _created_partial_types.add(name)


class FeedbackInterest(bases.BaseFeedbackInterest):
    """Represents a FeedbackInterest record"""

    feedback_id: _str
    interest_id: _int
    is_required: _bool
    created_at: datetime.datetime
    feedback: Optional['models.JobFeedback'] = None
    interest: Optional['models.Interest'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.FeedbackInterestKeys']] = None,
        exclude: Optional[Iterable['types.FeedbackInterestKeys']] = None,
        required: Optional[Iterable['types.FeedbackInterestKeys']] = None,
        optional: Optional[Iterable['types.FeedbackInterestKeys']] = None,
        relations: Optional[Mapping['types.FeedbackInterestRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.FeedbackInterestKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _FeedbackInterest_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _FeedbackInterest_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _FeedbackInterest_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _FeedbackInterest_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _FeedbackInterest_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _FeedbackInterest_relational_fields:
                        raise errors.UnknownRelationalFieldError('FeedbackInterest', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid FeedbackInterest / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'FeedbackInterest',
            }
        )
        _created_partial_types.add(name)


class History(bases.BaseHistory):
    """Represents a History record"""

    history_id: _str
    job_id: _int
    dreamer_id: _str
    good: _bool
    bad: _bool
    save: _bool
    created_at: datetime.datetime
    job: Optional['models.Job'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.HistoryKeys']] = None,
        exclude: Optional[Iterable['types.HistoryKeys']] = None,
        required: Optional[Iterable['types.HistoryKeys']] = None,
        optional: Optional[Iterable['types.HistoryKeys']] = None,
        relations: Optional[Mapping['types.HistoryRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.HistoryKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _History_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _History_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _History_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _History_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _History_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _History_relational_fields:
                        raise errors.UnknownRelationalFieldError('History', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid History / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'History',
            }
        )
        _created_partial_types.add(name)


class Dreamer(bases.BaseDreamer):
    """Represents a Dreamer record"""

    dreamer_id: _str
    organization_id: Optional[_int] = None
    login_id: _str
    name_family: _str
    name_given: _str
    grade: Optional[_str] = None
    last_login_at: Optional[datetime.datetime] = None
    created_at: datetime.datetime
    updated_at: datetime.datetime
    dreamer_group_members: Optional[List['models.DreamerGroupMember']] = None
    user_initial_answers: Optional[List['models.UserInitialAnswer']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DreamerKeys']] = None,
        exclude: Optional[Iterable['types.DreamerKeys']] = None,
        required: Optional[Iterable['types.DreamerKeys']] = None,
        optional: Optional[Iterable['types.DreamerKeys']] = None,
        relations: Optional[Mapping['types.DreamerRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DreamerKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Dreamer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Dreamer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Dreamer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Dreamer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Dreamer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Dreamer_relational_fields:
                        raise errors.UnknownRelationalFieldError('Dreamer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Dreamer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Dreamer',
            }
        )
        _created_partial_types.add(name)


class DreamerGroup(bases.BaseDreamerGroup):
    """Represents a DreamerGroup record"""

    group_id: _str
    name: _str
    description: Optional[_str] = None
    created_at: datetime.datetime
    updated_at: datetime.datetime
    dreamer_group_members: Optional[List['models.DreamerGroupMember']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DreamerGroupKeys']] = None,
        exclude: Optional[Iterable['types.DreamerGroupKeys']] = None,
        required: Optional[Iterable['types.DreamerGroupKeys']] = None,
        optional: Optional[Iterable['types.DreamerGroupKeys']] = None,
        relations: Optional[Mapping['types.DreamerGroupRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DreamerGroupKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DreamerGroup_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DreamerGroup_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DreamerGroup_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DreamerGroup_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DreamerGroup_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DreamerGroup_relational_fields:
                        raise errors.UnknownRelationalFieldError('DreamerGroup', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DreamerGroup / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DreamerGroup',
            }
        )
        _created_partial_types.add(name)


class DreamerGroupMember(bases.BaseDreamerGroupMember):
    """Represents a DreamerGroupMember record"""

    id: _str
    group_id: _str
    dreamer_id: _str
    joined_at: datetime.datetime
    created_at: datetime.datetime
    group: Optional['models.DreamerGroup'] = None
    dreamer: Optional['models.Dreamer'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.DreamerGroupMemberKeys']] = None,
        exclude: Optional[Iterable['types.DreamerGroupMemberKeys']] = None,
        required: Optional[Iterable['types.DreamerGroupMemberKeys']] = None,
        optional: Optional[Iterable['types.DreamerGroupMemberKeys']] = None,
        relations: Optional[Mapping['types.DreamerGroupMemberRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.DreamerGroupMemberKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _DreamerGroupMember_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _DreamerGroupMember_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _DreamerGroupMember_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _DreamerGroupMember_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _DreamerGroupMember_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _DreamerGroupMember_relational_fields:
                        raise errors.UnknownRelationalFieldError('DreamerGroupMember', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid DreamerGroupMember / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'DreamerGroupMember',
            }
        )
        _created_partial_types.add(name)


class InitQuestion(bases.BaseInitQuestion):
    """Represents a InitQuestion record"""

    question_id: _str
    version: _int
    category: _str
    question_text: _str
    question_order: _int
    is_active: _bool
    created_at: datetime.datetime
    updated_at: datetime.datetime
    init_question_options: Optional[List['models.InitQuestionOption']] = None
    user_initial_answers: Optional[List['models.UserInitialAnswer']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.InitQuestionKeys']] = None,
        exclude: Optional[Iterable['types.InitQuestionKeys']] = None,
        required: Optional[Iterable['types.InitQuestionKeys']] = None,
        optional: Optional[Iterable['types.InitQuestionKeys']] = None,
        relations: Optional[Mapping['types.InitQuestionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.InitQuestionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _InitQuestion_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _InitQuestion_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _InitQuestion_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _InitQuestion_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _InitQuestion_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _InitQuestion_relational_fields:
                        raise errors.UnknownRelationalFieldError('InitQuestion', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid InitQuestion / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'InitQuestion',
            }
        )
        _created_partial_types.add(name)


class InitQuestionOption(bases.BaseInitQuestionOption):
    """Represents a InitQuestionOption record"""

    option_id: _str
    question_id: _str
    option_order: _int
    option_text: _str
    created_at: datetime.datetime
    question: Optional['models.InitQuestion'] = None
    user_initial_answers: Optional[List['models.UserInitialAnswer']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.InitQuestionOptionKeys']] = None,
        exclude: Optional[Iterable['types.InitQuestionOptionKeys']] = None,
        required: Optional[Iterable['types.InitQuestionOptionKeys']] = None,
        optional: Optional[Iterable['types.InitQuestionOptionKeys']] = None,
        relations: Optional[Mapping['types.InitQuestionOptionRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.InitQuestionOptionKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _InitQuestionOption_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _InitQuestionOption_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _InitQuestionOption_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _InitQuestionOption_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _InitQuestionOption_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _InitQuestionOption_relational_fields:
                        raise errors.UnknownRelationalFieldError('InitQuestionOption', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid InitQuestionOption / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'InitQuestionOption',
            }
        )
        _created_partial_types.add(name)


class UserInitialAnswer(bases.BaseUserInitialAnswer):
    """Represents a UserInitialAnswer record"""

    answer_id: _str
    dreamer_id: _str
    question_id: _str
    option_id: _str
    question_version: _int
    answered_at: datetime.datetime
    created_at: datetime.datetime
    updated_at: datetime.datetime
    dreamer: Optional['models.Dreamer'] = None
    question: Optional['models.InitQuestion'] = None
    option: Optional['models.InitQuestionOption'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.UserInitialAnswerKeys']] = None,
        exclude: Optional[Iterable['types.UserInitialAnswerKeys']] = None,
        required: Optional[Iterable['types.UserInitialAnswerKeys']] = None,
        optional: Optional[Iterable['types.UserInitialAnswerKeys']] = None,
        relations: Optional[Mapping['types.UserInitialAnswerRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.UserInitialAnswerKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _UserInitialAnswer_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _UserInitialAnswer_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _UserInitialAnswer_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _UserInitialAnswer_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _UserInitialAnswer_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _UserInitialAnswer_relational_fields:
                        raise errors.UnknownRelationalFieldError('UserInitialAnswer', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid UserInitialAnswer / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'UserInitialAnswer',
            }
        )
        _created_partial_types.add(name)


class Conversation(bases.BaseConversation):
    """Represents a Conversation record"""

    conversation_id: _str
    owner_id: _str
    job_id: _str
    job_name: _str
    assistant_name: Optional[_str] = None
    assistant_gender: Optional[_str] = None
    last_message_at: datetime.datetime
    share_type: 'enums.ShareType'
    created_at: datetime.datetime
    updated_at: datetime.datetime
    messages: Optional[List['models.Message']] = None
    conversation_participants: Optional[List['models.ConversationParticipant']] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ConversationKeys']] = None,
        exclude: Optional[Iterable['types.ConversationKeys']] = None,
        required: Optional[Iterable['types.ConversationKeys']] = None,
        optional: Optional[Iterable['types.ConversationKeys']] = None,
        relations: Optional[Mapping['types.ConversationRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ConversationKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Conversation_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Conversation_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Conversation_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Conversation_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Conversation_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Conversation_relational_fields:
                        raise errors.UnknownRelationalFieldError('Conversation', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Conversation / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Conversation',
            }
        )
        _created_partial_types.add(name)


class Message(bases.BaseMessage):
    """Represents a Message record"""

    message_id: _str
    conversation_id: _str
    sender_id: _str
    role: 'enums.RoleType'
    text_content: _str
    created_at: datetime.datetime
    updated_at: datetime.datetime
    conversation: Optional['models.Conversation'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.MessageKeys']] = None,
        exclude: Optional[Iterable['types.MessageKeys']] = None,
        required: Optional[Iterable['types.MessageKeys']] = None,
        optional: Optional[Iterable['types.MessageKeys']] = None,
        relations: Optional[Mapping['types.MessageRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.MessageKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _Message_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _Message_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _Message_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _Message_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _Message_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _Message_relational_fields:
                        raise errors.UnknownRelationalFieldError('Message', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid Message / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'Message',
            }
        )
        _created_partial_types.add(name)


class ConversationParticipant(bases.BaseConversationParticipant):
    """Represents a ConversationParticipant record"""

    id: _str
    conversation_id: _str
    user_id: _str
    joined_at: datetime.datetime
    conversation: Optional['models.Conversation'] = None

    # take *args and **kwargs so that other metaclasses can define arguments
    def __init_subclass__(
        cls,
        *args: Any,
        warn_subclass: Optional[bool] = None,
        **kwargs: Any,
    ) -> None:
        super().__init_subclass__()
        if warn_subclass is not None:
            warnings.warn(
                'The `warn_subclass` argument is deprecated as it is no longer necessary and will be removed in the next release',
                DeprecationWarning,
                stacklevel=3,
            )


    @staticmethod
    def create_partial(
        name: str,
        include: Optional[Iterable['types.ConversationParticipantKeys']] = None,
        exclude: Optional[Iterable['types.ConversationParticipantKeys']] = None,
        required: Optional[Iterable['types.ConversationParticipantKeys']] = None,
        optional: Optional[Iterable['types.ConversationParticipantKeys']] = None,
        relations: Optional[Mapping['types.ConversationParticipantRelationalFieldKeys', str]] = None,
        exclude_relational_fields: bool = False,
    ) -> None:
        if not os.environ.get('PRISMA_GENERATOR_INVOCATION'):
            raise RuntimeError(
                'Attempted to create a partial type outside of client generation.'
            )

        if name in _created_partial_types:
            raise ValueError(f'Partial type "{name}" has already been created.')

        if include is not None:
            if exclude is not None:
                raise TypeError('Exclude and include are mutually exclusive.')
            if exclude_relational_fields is True:
                raise TypeError('Include and exclude_relational_fields=True are mutually exclusive.')

        if required and optional:
            shared = set(required) & set(optional)
            if shared:
                raise ValueError(f'Cannot make the same field(s) required and optional {shared}')

        if exclude_relational_fields and relations:
            raise ValueError(
                'exclude_relational_fields and relations are mutually exclusive'
            )

        fields: Dict['types.ConversationParticipantKeys', PartialModelField] = OrderedDict()

        try:
            if include:
                for field in include:
                    fields[field] = _ConversationParticipant_fields[field].copy()
            elif exclude:
                for field in exclude:
                    if field not in _ConversationParticipant_fields:
                        raise KeyError(field)

                fields = {
                    key: data.copy()
                    for key, data in _ConversationParticipant_fields.items()
                    if key not in exclude
                }
            else:
                fields = {
                    key: data.copy()
                    for key, data in _ConversationParticipant_fields.items()
                }

            if required:
                for field in required:
                    fields[field]['optional'] = False

            if optional:
                for field in optional:
                    fields[field]['optional'] = True

            if exclude_relational_fields:
                fields = {
                    key: data
                    for key, data in fields.items()
                    if key not in _ConversationParticipant_relational_fields
                }

            if relations:
                for field, type_ in relations.items():
                    if field not in _ConversationParticipant_relational_fields:
                        raise errors.UnknownRelationalFieldError('ConversationParticipant', field)

                    # TODO: this method of validating types is not ideal
                    # as it means we cannot two create partial types that
                    # reference each other
                    if type_ not in _created_partial_types:
                        raise ValueError(
                            f'Unknown partial type: "{type_}". '
                            f'Did you remember to generate the {type_} type before this one?'
                        )

                    # TODO: support non prisma.partials models
                    info = fields[field]
                    if info['is_list']:
                        info['type'] = f'List[\'partials.{type_}\']'
                    else:
                        info['type'] = f'\'partials.{type_}\''
        except KeyError as exc:
            raise ValueError(
                f'{exc.args[0]} is not a valid ConversationParticipant / {name} field.'
            ) from None

        models = partial_models_ctx.get()
        models.append(
            {
                'name': name,
                'fields': cast(Mapping[str, PartialModelField], fields),
                'from_model': 'ConversationParticipant',
            }
        )
        _created_partial_types.add(name)



_Industry_relational_fields: Set[str] = {
        'jobs',
    }
_Industry_fields: Dict['types.IndustryKeys', PartialModelField] = OrderedDict(
    [
        ('industry_id', {
            'name': 'industry_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('jobs', {
            'name': 'jobs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Job\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_JobCategory_relational_fields: Set[str] = {
        'jobs',
    }
_JobCategory_fields: Dict['types.JobCategoryKeys', PartialModelField] = OrderedDict(
    [
        ('category_id', {
            'name': 'category_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('jobs', {
            'name': 'jobs',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Job\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Skill_relational_fields: Set[str] = {
        'feedback_skills',
    }
_Skill_fields: Dict['types.SkillKeys', PartialModelField] = OrderedDict(
    [
        ('skill_id', {
            'name': 'skill_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback_skills', {
            'name': 'feedback_skills',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackSkill\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Certification_relational_fields: Set[str] = {
        'feedback_certifications',
    }
_Certification_fields: Dict['types.CertificationKeys', PartialModelField] = OrderedDict(
    [
        ('certification_id', {
            'name': 'certification_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback_certifications', {
            'name': 'feedback_certifications',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackCertification\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Company_relational_fields: Set[str] = {
        'feedback_companies',
    }
_Company_fields: Dict['types.CompanyKeys', PartialModelField] = OrderedDict(
    [
        ('company_id', {
            'name': 'company_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback_companies', {
            'name': 'feedback_companies',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackCompany\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Talent_relational_fields: Set[str] = {
        'feedback_talents',
    }
_Talent_fields: Dict['types.TalentKeys', PartialModelField] = OrderedDict(
    [
        ('talent_id', {
            'name': 'talent_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback_talents', {
            'name': 'feedback_talents',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackTalent\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Interest_relational_fields: Set[str] = {
        'feedback_interests',
    }
_Interest_fields: Dict['types.InterestKeys', PartialModelField] = OrderedDict(
    [
        ('interest_id', {
            'name': 'interest_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback_interests', {
            'name': 'feedback_interests',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackInterest\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Job_relational_fields: Set[str] = {
        'industry',
        'category',
        'job_images',
        'job_feedbacks',
        'histories',
    }
_Job_fields: Dict['types.JobKeys', PartialModelField] = OrderedDict(
    [
        ('job_id', {
            'name': 'job_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('industry_id', {
            'name': 'industry_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('category_id', {
            'name': 'category_id',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('industry', {
            'name': 'industry',
            'is_list': False,
            'optional': True,
            'type': 'models.Industry',
            'is_relational': True,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': True,
            'type': 'models.JobCategory',
            'is_relational': True,
            'documentation': None,
        }),
        ('job_images', {
            'name': 'job_images',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.JobImage\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('job_feedbacks', {
            'name': 'job_feedbacks',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.JobFeedback\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('histories', {
            'name': 'histories',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.History\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_JobImage_relational_fields: Set[str] = {
        'job',
    }
_JobImage_fields: Dict['types.JobImageKeys', PartialModelField] = OrderedDict(
    [
        ('img_id', {
            'name': 'img_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('job_id', {
            'name': 'job_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('img_url', {
            'name': 'img_url',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('alt', {
            'name': 'alt',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('job', {
            'name': 'job',
            'is_list': False,
            'optional': True,
            'type': 'models.Job',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_JobFeedback_relational_fields: Set[str] = {
        'job',
        'feedback_skill',
        'feedback_certification',
        'feedback_company',
        'feedback_talent',
        'feedback_interest',
    }
_JobFeedback_fields: Dict['types.JobFeedbackKeys', PartialModelField] = OrderedDict(
    [
        ('feedback_id', {
            'name': 'feedback_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('job_id', {
            'name': 'job_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('salary', {
            'name': 'salary',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('level', {
            'name': 'level',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('end_time', {
            'name': 'end_time',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('holiday', {
            'name': 'holiday',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('overtime_hours', {
            'name': 'overtime_hours',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('age', {
            'name': 'age',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('tenure_years', {
            'name': 'tenure_years',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('marriage_age', {
            'name': 'marriage_age',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('gender_ratio', {
            'name': 'gender_ratio',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('romance_rate', {
            'name': 'romance_rate',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('social_signification', {
            'name': 'social_signification',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('personality_traits', {
            'name': 'personality_traits',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('growth_opportunities', {
            'name': 'growth_opportunities',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('wrong_image', {
            'name': 'wrong_image',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('uniform', {
            'name': 'uniform',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('work_life_balance', {
            'name': 'work_life_balance',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('future_outlook', {
            'name': 'future_outlook',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('rarity', {
            'name': 'rarity',
            'is_list': False,
            'optional': True,
            'type': '_float',
            'is_relational': False,
            'documentation': None,
        }),
        ('scandal_history', {
            'name': 'scandal_history',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('focus_on_education', {
            'name': 'focus_on_education',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('focus_on_achievements', {
            'name': 'focus_on_achievements',
            'is_list': False,
            'optional': True,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('appeal_points', {
            'name': 'appeal_points',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('daily_routine', {
            'name': 'daily_routine',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('comments', {
            'name': 'comments',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('job', {
            'name': 'job',
            'is_list': False,
            'optional': True,
            'type': 'models.Job',
            'is_relational': True,
            'documentation': None,
        }),
        ('feedback_skill', {
            'name': 'feedback_skill',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackSkill\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('feedback_certification', {
            'name': 'feedback_certification',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackCertification\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('feedback_company', {
            'name': 'feedback_company',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackCompany\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('feedback_talent', {
            'name': 'feedback_talent',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackTalent\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('feedback_interest', {
            'name': 'feedback_interest',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.FeedbackInterest\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FeedbackSkill_relational_fields: Set[str] = {
        'feedback',
        'skill',
    }
_FeedbackSkill_fields: Dict['types.FeedbackSkillKeys', PartialModelField] = OrderedDict(
    [
        ('feedback_id', {
            'name': 'feedback_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('skill_id', {
            'name': 'skill_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('is_required', {
            'name': 'is_required',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback', {
            'name': 'feedback',
            'is_list': False,
            'optional': True,
            'type': 'models.JobFeedback',
            'is_relational': True,
            'documentation': None,
        }),
        ('skill', {
            'name': 'skill',
            'is_list': False,
            'optional': True,
            'type': 'models.Skill',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FeedbackCertification_relational_fields: Set[str] = {
        'feedback',
        'certification',
    }
_FeedbackCertification_fields: Dict['types.FeedbackCertificationKeys', PartialModelField] = OrderedDict(
    [
        ('feedback_id', {
            'name': 'feedback_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('certification_id', {
            'name': 'certification_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('is_required', {
            'name': 'is_required',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback', {
            'name': 'feedback',
            'is_list': False,
            'optional': True,
            'type': 'models.JobFeedback',
            'is_relational': True,
            'documentation': None,
        }),
        ('certification', {
            'name': 'certification',
            'is_list': False,
            'optional': True,
            'type': 'models.Certification',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FeedbackCompany_relational_fields: Set[str] = {
        'feedback',
        'company',
    }
_FeedbackCompany_fields: Dict['types.FeedbackCompanyKeys', PartialModelField] = OrderedDict(
    [
        ('feedback_id', {
            'name': 'feedback_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('company_id', {
            'name': 'company_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback', {
            'name': 'feedback',
            'is_list': False,
            'optional': True,
            'type': 'models.JobFeedback',
            'is_relational': True,
            'documentation': None,
        }),
        ('company', {
            'name': 'company',
            'is_list': False,
            'optional': True,
            'type': 'models.Company',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FeedbackTalent_relational_fields: Set[str] = {
        'feedback',
        'talent',
    }
_FeedbackTalent_fields: Dict['types.FeedbackTalentKeys', PartialModelField] = OrderedDict(
    [
        ('feedback_id', {
            'name': 'feedback_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('talent_id', {
            'name': 'talent_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('is_required', {
            'name': 'is_required',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback', {
            'name': 'feedback',
            'is_list': False,
            'optional': True,
            'type': 'models.JobFeedback',
            'is_relational': True,
            'documentation': None,
        }),
        ('talent', {
            'name': 'talent',
            'is_list': False,
            'optional': True,
            'type': 'models.Talent',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_FeedbackInterest_relational_fields: Set[str] = {
        'feedback',
        'interest',
    }
_FeedbackInterest_fields: Dict['types.FeedbackInterestKeys', PartialModelField] = OrderedDict(
    [
        ('feedback_id', {
            'name': 'feedback_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('interest_id', {
            'name': 'interest_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('is_required', {
            'name': 'is_required',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('feedback', {
            'name': 'feedback',
            'is_list': False,
            'optional': True,
            'type': 'models.JobFeedback',
            'is_relational': True,
            'documentation': None,
        }),
        ('interest', {
            'name': 'interest',
            'is_list': False,
            'optional': True,
            'type': 'models.Interest',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_History_relational_fields: Set[str] = {
        'job',
    }
_History_fields: Dict['types.HistoryKeys', PartialModelField] = OrderedDict(
    [
        ('history_id', {
            'name': 'history_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('job_id', {
            'name': 'job_id',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('dreamer_id', {
            'name': 'dreamer_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('good', {
            'name': 'good',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('bad', {
            'name': 'bad',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('save', {
            'name': 'save',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('job', {
            'name': 'job',
            'is_list': False,
            'optional': True,
            'type': 'models.Job',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Dreamer_relational_fields: Set[str] = {
        'dreamer_group_members',
        'user_initial_answers',
    }
_Dreamer_fields: Dict['types.DreamerKeys', PartialModelField] = OrderedDict(
    [
        ('dreamer_id', {
            'name': 'dreamer_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('organization_id', {
            'name': 'organization_id',
            'is_list': False,
            'optional': True,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('login_id', {
            'name': 'login_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name_family', {
            'name': 'name_family',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name_given', {
            'name': 'name_given',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('grade', {
            'name': 'grade',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('last_login_at', {
            'name': 'last_login_at',
            'is_list': False,
            'optional': True,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('dreamer_group_members', {
            'name': 'dreamer_group_members',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DreamerGroupMember\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('user_initial_answers', {
            'name': 'user_initial_answers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserInitialAnswer\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DreamerGroup_relational_fields: Set[str] = {
        'dreamer_group_members',
    }
_DreamerGroup_fields: Dict['types.DreamerGroupKeys', PartialModelField] = OrderedDict(
    [
        ('group_id', {
            'name': 'group_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('name', {
            'name': 'name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('description', {
            'name': 'description',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('dreamer_group_members', {
            'name': 'dreamer_group_members',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.DreamerGroupMember\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_DreamerGroupMember_relational_fields: Set[str] = {
        'group',
        'dreamer',
    }
_DreamerGroupMember_fields: Dict['types.DreamerGroupMemberKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('group_id', {
            'name': 'group_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('dreamer_id', {
            'name': 'dreamer_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('joined_at', {
            'name': 'joined_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('group', {
            'name': 'group',
            'is_list': False,
            'optional': True,
            'type': 'models.DreamerGroup',
            'is_relational': True,
            'documentation': None,
        }),
        ('dreamer', {
            'name': 'dreamer',
            'is_list': False,
            'optional': True,
            'type': 'models.Dreamer',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_InitQuestion_relational_fields: Set[str] = {
        'init_question_options',
        'user_initial_answers',
    }
_InitQuestion_fields: Dict['types.InitQuestionKeys', PartialModelField] = OrderedDict(
    [
        ('question_id', {
            'name': 'question_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('version', {
            'name': 'version',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('category', {
            'name': 'category',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('question_text', {
            'name': 'question_text',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('question_order', {
            'name': 'question_order',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('is_active', {
            'name': 'is_active',
            'is_list': False,
            'optional': False,
            'type': '_bool',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('init_question_options', {
            'name': 'init_question_options',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.InitQuestionOption\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('user_initial_answers', {
            'name': 'user_initial_answers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserInitialAnswer\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_InitQuestionOption_relational_fields: Set[str] = {
        'question',
        'user_initial_answers',
    }
_InitQuestionOption_fields: Dict['types.InitQuestionOptionKeys', PartialModelField] = OrderedDict(
    [
        ('option_id', {
            'name': 'option_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('question_id', {
            'name': 'question_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('option_order', {
            'name': 'option_order',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('option_text', {
            'name': 'option_text',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('question', {
            'name': 'question',
            'is_list': False,
            'optional': True,
            'type': 'models.InitQuestion',
            'is_relational': True,
            'documentation': None,
        }),
        ('user_initial_answers', {
            'name': 'user_initial_answers',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.UserInitialAnswer\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_UserInitialAnswer_relational_fields: Set[str] = {
        'dreamer',
        'question',
        'option',
    }
_UserInitialAnswer_fields: Dict['types.UserInitialAnswerKeys', PartialModelField] = OrderedDict(
    [
        ('answer_id', {
            'name': 'answer_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('dreamer_id', {
            'name': 'dreamer_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('question_id', {
            'name': 'question_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('option_id', {
            'name': 'option_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('question_version', {
            'name': 'question_version',
            'is_list': False,
            'optional': False,
            'type': '_int',
            'is_relational': False,
            'documentation': None,
        }),
        ('answered_at', {
            'name': 'answered_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('dreamer', {
            'name': 'dreamer',
            'is_list': False,
            'optional': True,
            'type': 'models.Dreamer',
            'is_relational': True,
            'documentation': None,
        }),
        ('question', {
            'name': 'question',
            'is_list': False,
            'optional': True,
            'type': 'models.InitQuestion',
            'is_relational': True,
            'documentation': None,
        }),
        ('option', {
            'name': 'option',
            'is_list': False,
            'optional': True,
            'type': 'models.InitQuestionOption',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Conversation_relational_fields: Set[str] = {
        'messages',
        'conversation_participants',
    }
_Conversation_fields: Dict['types.ConversationKeys', PartialModelField] = OrderedDict(
    [
        ('conversation_id', {
            'name': 'conversation_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('owner_id', {
            'name': 'owner_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('job_id', {
            'name': 'job_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('job_name', {
            'name': 'job_name',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('assistant_name', {
            'name': 'assistant_name',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('assistant_gender', {
            'name': 'assistant_gender',
            'is_list': False,
            'optional': True,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('last_message_at', {
            'name': 'last_message_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('share_type', {
            'name': 'share_type',
            'is_list': False,
            'optional': False,
            'type': 'enums.ShareType',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('messages', {
            'name': 'messages',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.Message\']',
            'is_relational': True,
            'documentation': None,
        }),
        ('conversation_participants', {
            'name': 'conversation_participants',
            'is_list': True,
            'optional': True,
            'type': 'List[\'models.ConversationParticipant\']',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_Message_relational_fields: Set[str] = {
        'conversation',
    }
_Message_fields: Dict['types.MessageKeys', PartialModelField] = OrderedDict(
    [
        ('message_id', {
            'name': 'message_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('conversation_id', {
            'name': 'conversation_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('sender_id', {
            'name': 'sender_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('role', {
            'name': 'role',
            'is_list': False,
            'optional': False,
            'type': 'enums.RoleType',
            'is_relational': False,
            'documentation': None,
        }),
        ('text_content', {
            'name': 'text_content',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('created_at', {
            'name': 'created_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('updated_at', {
            'name': 'updated_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('conversation', {
            'name': 'conversation',
            'is_list': False,
            'optional': True,
            'type': 'models.Conversation',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)

_ConversationParticipant_relational_fields: Set[str] = {
        'conversation',
    }
_ConversationParticipant_fields: Dict['types.ConversationParticipantKeys', PartialModelField] = OrderedDict(
    [
        ('id', {
            'name': 'id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('conversation_id', {
            'name': 'conversation_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('user_id', {
            'name': 'user_id',
            'is_list': False,
            'optional': False,
            'type': '_str',
            'is_relational': False,
            'documentation': None,
        }),
        ('joined_at', {
            'name': 'joined_at',
            'is_list': False,
            'optional': False,
            'type': 'datetime.datetime',
            'is_relational': False,
            'documentation': None,
        }),
        ('conversation', {
            'name': 'conversation',
            'is_list': False,
            'optional': True,
            'type': 'models.Conversation',
            'is_relational': True,
            'documentation': None,
        }),
    ],
)



# we have to import ourselves as relation types are namespaced to models
# e.g. models.Post
from . import models, actions

# required to support relationships between models
model_rebuild(Industry)
model_rebuild(JobCategory)
model_rebuild(Skill)
model_rebuild(Certification)
model_rebuild(Company)
model_rebuild(Talent)
model_rebuild(Interest)
model_rebuild(Job)
model_rebuild(JobImage)
model_rebuild(JobFeedback)
model_rebuild(FeedbackSkill)
model_rebuild(FeedbackCertification)
model_rebuild(FeedbackCompany)
model_rebuild(FeedbackTalent)
model_rebuild(FeedbackInterest)
model_rebuild(History)
model_rebuild(Dreamer)
model_rebuild(DreamerGroup)
model_rebuild(DreamerGroupMember)
model_rebuild(InitQuestion)
model_rebuild(InitQuestionOption)
model_rebuild(UserInitialAnswer)
model_rebuild(Conversation)
model_rebuild(Message)
model_rebuild(ConversationParticipant)
